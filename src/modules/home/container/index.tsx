import { useNewsQuery } from "../../../hooks/useQuery/news";

const HomeContainer = () => {
  // const { data } = useNewsQuery();

  // console.log(data);

  return <div></div>;
};

export default HomeContainer;
